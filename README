----
-- The following is some basic syntax of brsh
```
func name(
  printf(
    brSH!
  ) # acts as printf "%s\\n" "brSH!" 
  (var) = 1 # sets (var) as 1
  printf((var)) # outputs (var)/1
  (1=1) # exits 0 (true)
  (
    # something something
  ) # code block
)
```
----
